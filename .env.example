# Anthropic API 키 — 서버(Vercel Function)에서만 사용됩니다.
# ⚠️ VITE_ 접두어를 절대 붙이지 마세요. (붙이면 프론트 번들에 노출됨)
# 로컬: 이 파일을 .env 로 복사한 뒤 값을 채우고 `vercel dev` 로 실행
# 배포: Vercel 프로젝트 → Settings → Environment Variables 에 등록
ANTHROPIC_API_KEY=
